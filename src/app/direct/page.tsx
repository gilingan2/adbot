import type { Metadata } from "next";
import DirectView from "@/components/view/direct/view";
export const metadata = {
    title: "Direct Page",
  }
  export default function DirectPage() {
    return (
     <DirectView/>
    )
  }
  
  